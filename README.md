# URL-Shortner
A simple Flask-based URL shortener that stores shortened IDs and original URLs in SQLite, exposes a /shorten JSON API to create short links, and redirects via /s/&lt;id> with proper error handling
